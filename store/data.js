export const state = () => ({})

export const getters = {}

export const mutations = {
  CLEAR_STATE(state) {
    // Clear localStorage
    // storage.clear()

    // Reset State
    state.activeProfile = []
  },
}

export const actions = {}
